<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled action for checking blood bank availability -->
        <record id="ir_cron_scheduler_blood_availability" model="ir.cron">
            <field name="name"></field>
            <field name="model_id" ref="base_hospital_management.model_blood_bank"/>  <!-- 👈 add module prefix -->
            <field name="state">code</field>
            <field name="code">model.action_change_availability()</field>
            <field name="interval_number">3</field>
            <field name="interval_type">months</field>
            <!-- <field name="numbercall">-1</field> -->
            <field name="active">True</field>  <!-- 👈 optional but recommended -->
        </record>
    </data>
</odoo>
