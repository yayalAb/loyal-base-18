<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--Patient Lab Test form view-->
    <record id="patient_lab_test_view_form" model="ir.ui.view">
        <field name="name">patient.lab.test.view.form</field>
        <field name="model">patient.lab.test</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar"/>
                    <button name="action_create_invoice"
                            string="Create Invoice"
                            class="oe_highlight"
                            groups="base_hospital_management.base_hospital_management_group_lab_assistant"
                            invisible="state != 'draft' or invoiced == True"
                            type="object"/>
                    <button name="action_start_test"
                            string="Start Test"
                            class="oe_highlight"
                            invisible="invoiced != True or  state != 'invoiced'"
                            groups="base_hospital_management.base_hospital_management_group_lab_assistant"
                            type="object"/>
                    <button name="action_test_end"
                            string="End Test"
                            class="oe_highlight"
                            invisible="state != 'test'"
                            groups="base_hospital_management.base_hospital_management_group_lab_assistant"
                            type="object"/>

                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_invoice" type="object"
                                class="oe_stat_button" icon="fa-edit"
                                invisible="invoice_count == 0">
                            <field string="Invoice" name="invoice_count"
                                   widget="statinfo"/>
                        </button>
                        <!-- <button name="action_view_sale_order" type="object"
                                class="oe_stat_button" icon="fa-usd"
                                invisible="sale_count == 0">
                            <field string="Sale Order" name="sale_count"
                                   widget="statinfo"/>
                        </button> -->
                    </div>
                    <field name="name" readonly="1"/>
                    <group>
                        <group>
                            <!-- <field name="test_id"/> -->
                            <field name="patient_id"/>
                            <field name="patient_type"  widget="radio" options="{'horizontal': true}"/>
                            <field name="invoiced" invisible="1"/>
                        </group>
                        <group>
                            <field name="doctor_id"/>
                            <field name="date"/>
                            <!-- <field name="lab_id"
                                   options="{'no_create': True, 'no_edit': True}"/> -->
                        </group>
                    </group>
                    <notebook>
                        <page string="Test Details">
                            <field name="test_ids"/>
                        </page>
                        <!-- <page string="Medicines">
                            <field name="medicine_ids">
                                <list editable="bottom">
                                    <field name="medicine_id"/>
                                    <field name="qty_available"/>
                                    <field name="quantity"/>
                                    <field name="sub_total"/>
                                </list>
                            </field>
                        </page> -->
                        <page name="Result"
                              invisible="state == 'draft'">
                            <field name="result_ids">
                                <list>
                                    <field name="test_id"/>
                                    <field name="lower_limit"/>
                                    <field name="result"/>
                                    <field name="upper_limit"/>
                                    <field name="uom_id"/>
                                    <!-- <field name="attachment" /> -->
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <!--    Patient Lab Test tree view-->
    <record id="patient_lab_test_view_tree" model="ir.ui.view">
        <field name="name">patient.lab.test.view.tree</field>
        <field name="model">patient.lab.test</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="patient_id"/>
                <field name="patient_type"/>
                <field name="doctor_id"/>
                <field name="date"/>
                <field name="state"/>
            </list>
        </field>
    </record>
        <record id="patient_lab_test_view_search" model="ir.ui.view">
        <field name="name">patient.lab.test.view.search</field>
        <field name="model">patient.lab.test</field>
        <field name="arch" type="xml">
            <search>
                 <filter string="Today's Draft Order" name="created_today_draft" domain="[('state','=','draft'),('create_date', '>=', context_today().strftime('%Y-%m-%d 00:00:00'))]"/>
                 <filter string="Today's Order" name="created_today" domain="[('create_date', '>=', context_today().strftime('%Y-%m-%d 00:00:00'))]"/>
                <filter string="Last 7 Days" name="last_7_days" domain="[('create_date', '>=', (context_today() - datetime.timedelta(days=7)).strftime('%Y-%m-%d 00:00:00'))]"/>
                <filter string="This Month" name="this_month" domain="[('create_date', '>=', context_today().strftime('%Y-%m-01 00:00:00'))]"/>
            </search>
        </field>
    </record>
    <!--    Patient Lab Test action-->
    <record id="patient_lab_test_action" model="ir.actions.act_window">
        <field name="name">Patient Lab Test</field>
        <field name="res_model">patient.lab.test</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_created_today_draft': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Laboratory Tests
            </p>
        </field>
    </record>
    <!--    Patient Lab Test menu-->
    <menuitem id="patient_lab_test_menu" name="Orders"
              parent="hospital_menu_laboratory" sequence="1"
              action="patient_lab_test_action"/>
</odoo>
