<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Purchase Request List (list) View -->
    <record id="view_purchase_request_list" model="ir.ui.view">
        <field name="name">purchase.request.list</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list string="Purchase Requests">
                <field name="name" />
                <field name="requested_by" />
                <field name="department_id" />
                <field name="requested_date" />
                <field name="warehouse_id" />
                <field name="state" widget="badge" decoration-success="state == 'approved'"
                    decoration-info="state == 'submitted'" decoration-muted="state == 'cancel'" />
            </list>
        </field>
    </record>

    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <!-- ADD THE NEW BUTTON -->
                    <button name="action_create_purchase_order" string="Create Purchase Order"
                        type="object" class="btn-primary" invisible="state != 'authorized'" />

                    <button name="action_submit" string="Submit" type="object" class="btn-primary"
                        invisible="state != 'draft'" />
                    <button name="action_approve" string="Approve" type="object" class="btn-primary"
                        invisible="state != 'submitted'" />
                    <button name="action_authorize" string="Authorize" type="object" class="btn-primary"
                        invisible="state != 'approved'" />
                    <button name="action_cancel" string="Cancel" type="object"
                        invisible="state in ['approved', 'ordered', 'cancel']" />

                    <!-- ADD THE NEW 'ordered' STATE TO THE STATUS BAR -->
                    <field name="state" widget="statusbar"
                        statusbar_visible="draft,submitted,approved,authorized,ordered" />
                </header>
                <sheet>
                    <!-- ADD THE SMART BUTTON BOX -->
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_purchase_orders"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-shopping-cart"
                            invisible="purchase_order_count == 0">
                            <field name="purchase_order_count" widget="statinfo"
                                string="Purchase Orders" />
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1" />
                        </h1>
                    </div>
                    <!-- ... (the rest of your form remains the same) ... -->
                    <group>
                        <group>
                            <field name="requested_by" readonly="state not in ['draft']" />
                            <field name="department_id" readonly="state not in ['draft']" />
                            <field name="store_request_id" readonly="1" options="{'no_open': True}" />
                        </group>
                        <group>
                            <field name="requested_date" readonly="state not in ['draft']" />
                            <field name="warehouse_id" string="Deliver To"
                                readonly="state not in ['draft']" />
                            <field name="factory_id" readonly="state not in ['draft']" />
                            <field name="purchase_type" widget="radio"
                                readonly="state not in ['draft']" />
                            <field name="approved_by" readonly="1" />
                        </group>
                    </group>
                    <group>
                        <field name="purpose"
                            readonly="state not in ['draft']" />
                    </group>
                    <notebook>
                        <page string="Products" name="request_lines">
                            <field name="request_line_ids" readonly="state not in ['draft']">
                                <list string="Products to Purchase" editable="bottom">
                                    <field name="product_id" options="{'no_create_edit': True}" />
                                    <field name="description" />
                                    <field name="product_uom_qty" string="Qty" />
                                    <field name="product_uom" string="UoM" />
                                    <field name="remark" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Your Action and Menu -->
    <record id="action_purchase_request" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_purchase_request"
        name="Purchase Requests"
        parent="store_request.menu_store_request_root"
        action="action_purchase_request" />

</odoo>