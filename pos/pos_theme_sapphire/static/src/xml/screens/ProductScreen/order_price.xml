<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-inherit="point_of_sale.OrderWidget" t-inherit-mode="extension">

        <xpath expr="//div[hasclass('order-summary')]" position="replace">
            <div t-if="taxTotals" class="order-summary d-flex flex-column gap-1 px-3 py-2 border-top fw-bolder lh-sm">
                 <div class="d-flex justify-content-between w-100  order_summery_color ">
                        Subtotal 
                        <span t-esc="formatMonetary(taxTotals.order_sign * taxTotals.order_total - taxTotals.order_sign * taxTotals.tax_amount_currency, {currencyId: taxTotals.currency_id})"
                              />
                </div>

                    
                <div t-if="taxTotals.has_tax_groups"
                         class="tax-info subentry d-flex justify-content-between w-100 fs-6 order_summery_color"
                    >
                        Taxes
                        <div id="order-widget-taxes">
                            <span t-esc="formatMonetary(taxTotals.order_sign * taxTotals.tax_amount_currency, {currencyId: taxTotals.currency_id})"
                                  />
                        </div>
                    </div>
                    <div class="d-flex justify-content-between w-100 fs-2 order_summery_color total_class">
                        Total
                        <span t-esc="formatMonetary(taxTotals.order_sign * taxTotals.order_total, {currencyId: taxTotals.currency_id})"
                              />
                    </div>
                    

            </div>
              
        </xpath>
    </t>
 </templates>
 